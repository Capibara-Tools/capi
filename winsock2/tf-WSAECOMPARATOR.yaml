summary: "The primary type for the enum, [`winsock2/_WSAEcomparator`], concerned with Winsock2 versioning semantics."
type: "[`winsock2/_WSAEcomparator`]"
associated_ref: "winsock2/_WSAEcomparator"
description: |
  One of several type definitions over the [`winsock2/_WSAEcomparator`] enum.
  However, this type definition is the primary alias and it's also referenced
  as a member of [`winsock2/_WSAVersion`] struct.
os_affinity: ["win"]

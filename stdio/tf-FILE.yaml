summary: "This is a type whose information represents a file stream."
type: "opaque struct reference"
associated_ref: ""
description: |
  The FILE type is considered an opaque data type, because it's implementation is vendor specific. Users aren't
  supposed to access the internal properties of the struct and should only ever use pointers to the type.
os_affinity: ["std"]
